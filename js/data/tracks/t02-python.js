// Internship 2: Python, Park Operations Tools.
IS.addCharacters({
  nadia: {
    name: 'Nadia Karimi', title: 'Engineering Manager, Park Operations Tools', role: 'Manager',
    bio: 'Nadia\'s team automates the thousand small things that keep a park open: staffing, lost & found, maintenance windows. Calm, organized, allergic to surprises.',
    look: { skin: '#c68b5f', hair: 'wavy', hairColor: '#2f2019', eyes: '#3b2618', top: 'blazer', topColor: '#3e6b48', bottom: 'pants', bottomColor: '#2b2622', shoes: 'loafers', accessory2: 'earrings' },
    chat: ['Automation is a promise: this will work at 6 AM when nobody is watching.', 'Tell me the risk before you tell me the fix.', 'A script without error handling is a surprise waiting for a holiday weekend.'],
  },
  kenji: {
    name: 'Kenji Watanabe', title: 'Senior Software Engineer (your mentor)', role: 'Mentor',
    bio: 'Kenji has written Python for fifteen years and still gets excited about a clean list comprehension. Patient, precise, loves type hints.',
    look: { skin: '#e7bf9c', hair: 'sidepart', hairColor: '#1c1714', eyes: '#3b2618', top: 'cardigan', topColor: '#8a6d4b', bottom: 'pants', bottomColor: '#5a4636', shoes: 'loafers', accessory: 'glasses' },
    chat: ['Readability counts. Future-you is your most important code reviewer.', 'When in doubt, write the test first.', 'The standard library has a function for that. It always does.'],
  },
  amara: {
    name: 'Amara Osei', title: 'Software Engineering Intern', role: 'Intern', reliability: 0.9,
    bio: 'Organized, warm, and the first to volunteer for the boring-but-important parts.',
    look: { skin: '#6b4029', hair: 'afro', hairColor: '#1c1714', eyes: '#3b2618', top: 'polo', topColor: '#d99a2b', bottom: 'pants', bottomColor: '#c9b18a', shoes: 'sneakers', accessory2: 'earrings' },
    chat: ['I color-code my to-do list. It\'s a lifestyle.', 'Want to pair on the scheduler? Two brains, one keyboard.'],
  },
  leo: {
    name: 'Leo Marchetti', title: 'Software Engineering Intern', role: 'Intern', reliability: 0.6,
    bio: 'Fast, clever, and chronically optimistic about how long things will take.',
    look: { skin: '#e7bf9c', hair: 'curly', hairColor: '#4a2e1c', eyes: '#5a3a22', facial: 'stubble', top: 'hoodie', topColor: '#a8412a', bottom: 'pants', bottomColor: '#2b2622', shoes: 'sneakers' },
    chat: ['Five minutes. It\'ll take five minutes. (It did not take five minutes.)', 'I wrote a one-liner that does everything. Nobody can read it, including me.'],
  },
  sofia: {
    name: 'Sofia Reyes', title: 'Data Engineering Intern', role: 'Intern', reliability: 0.95,
    bio: 'Quietly excellent. Her pull requests have tests for the tests.',
    look: { skin: '#d9a57c', hair: 'long', hairColor: '#1c1714', eyes: '#3b2618', top: 'button', topColor: '#f3ece0', bottom: 'skirt', bottomColor: '#5a4636', shoes: 'flats', accessory: 'glasses' },
    chat: ['Edge cases are just cases you haven\'t met yet.', 'I wrote a checklist for our demo. And a checklist for the checklist.'],
  },
  marco: {
    name: 'Marco Lindqvist', title: 'Operations Intern', role: 'Intern', reliability: 0.7,
    bio: 'Former ride operator turned engineer. Knows exactly how tools break at 7 AM on a holiday.',
    look: { skin: '#f8e1cf', hair: 'short', hairColor: '#c9a063', eyes: '#6b6f5c', facial: 'beard', top: 'hivis', bottom: 'pants', bottomColor: '#4b5a3a', shoes: 'boots', build: 'broad' },
    chat: ['Operators will find every bug. Usually at opening time.', 'If the kiosk needs instructions, the kiosk is wrong.'],
  },
});

IS.registerTrack({
  id: 'python', n: 2, icon: '🐍', lang: 'python', langLabel: 'Python', rate: 26,
  title: 'Software Engineering Intern', team: 'Park Operations Tools',
  blurb: 'Write Python that automates park operations: staff scheduling, maintenance windows, lost & found and guest comments.',
  skills: ['Python', 'Dicts, lists & comprehensions', 'Parsing messy input', 'Automation scripts'],
  channel: 'park-ops', lab: { name: 'Operations Control Room', art: 'data' },
  cast: { manager: 'nadia', mentor: 'kenji', interns: ['amara', 'leo', 'sofia', 'marco'] },
  groups: { g1: { name: 'Shift Scheduler Bot', members: ['amara', 'leo'] }, g2: { name: 'Lost & Found Matcher', members: ['sofia', 'marco'] } },
  incident: 'The staff check-in kiosk crashes for anyone whose badge ID contains letters.',
  scenario: { conflictA: 'a live demo on the real kiosk', conflictB: 'a recorded walkthrough', delay: 'the HR system export is broken', delayFix: 'a fake-data generator' },
  tasks: [
    {
      id: 'q1', type: 'quiz', title: 'Operations Safety & Guest Privacy', from: 'rosa', day: 1, due: { day: 1, minute: 180 }, effort: 20, kind: 'individual', category: 'Training',
      brief: '<p>Park operations tools touch guest and cast data. Pass this privacy and safety check (80%+).</p>',
      hints: ['Personal data is shared on a need-to-know basis only.', 'When a tool could affect safety, stop and escalate.'],
      wiki: '<b>Data Handbook:</b> Guest names, emails and photos are personal data. Never export them to personal devices. Report suspected leaks immediately.',
      peer: { who: 'amara', text: 'Privacy questions are basically "don\'t share it, report it, and ask first."' },
      questions: [
        { q: 'A guest\'s lost-item report includes their phone number. Your friend asks you to look someone up. You:', options: ['Look it up. It\'s harmless.', 'Decline: guest data is only for the lost & found process.', 'Share just the first name.', 'Ask the guest later if it was OK.'], answer: 1 },
        { q: 'You want to test your script with real staff schedules. You should:', options: ['Copy them to your personal laptop.', 'Use anonymized or fake data in a dev environment.', 'Email them to yourself.', 'Post them in the intern channel.'], answer: 1 },
        { q: 'Your automation could accidentally mark a ride as "inspected." What do you do before shipping?', options: ['Ship it and watch closely.', 'Get a review from operations and add safeguards and tests.', 'Only run it on weekends.', 'Nothing. Automation is reliable.'], answer: 1 },
        { q: 'You find a spreadsheet of guest emails on a shared drive with public access. You:', options: ['Ignore it.', 'Download a copy for safekeeping.', 'Report it to security/IT so it can be locked down.', 'Delete it without telling anyone.'], answer: 2 },
        { q: 'A script needs a password to reach an internal API. Where should it live?', options: ['In the code, with a comment.', 'In a secrets manager or environment variable.', 'In the README.', 'In a sticky note on your monitor.'], answer: 1 },
      ],
    },
    IS.T.intro({ channel: 'park-ops', team: 'Park Operations Tools', teamKey: 'operations' }),
    {
      id: 'c1', type: 'coding', title: 'Guest Comment Word Counter', from: 'mentor', day: 2, due: { day: 3, minute: 180 }, effort: 60, kind: 'individual', category: 'Coding',
      brief: '<p>Guest services wants to know which words show up most in comment cards. Write <code>count_words(text)</code> that returns a <b>dict</b> mapping each word to its count.</p><ul><li>lowercase everything</li><li>a word is a run of letters and apostrophes (<code>it\'s</code> is one word); punctuation and digits separate words</li><li>empty or blank text → <code>{}</code></li></ul>',
      fnName: 'count_words',
      starter: 'def count_words(text):\n    # TODO: return {word: count}\n    pass\n',
      tests: [{ args: ['Great ride! Great queue.'], expected: { great: 2, ride: 1, queue: 1 } }, { args: [''], expected: {} }],
      hidden: [{ args: ["It's FUN, it's fun"], expected: { "it's": 2, fun: 2 } }, { args: ['   '], expected: {} }, { args: ['A a a b'], expected: { a: 3, b: 1 } }, { args: ['wait2long'], expected: { wait: 1, long: 1 } }],
      hints: ['re.findall(r"[a-z\']+", text.lower()) gives you the words.', 'collections.Counter(words) or a dict with counts[w] = counts.get(w, 0) + 1.', 'Return a plain dict: dict(Counter(...)).'],
      wiki: '<b>Python docs:</b> <code>re.findall(pattern, s)</code> returns all matches. <code>collections.Counter</code> counts hashable items.',
      peer: { who: 'sofia', text: 'Regex for the words, Counter for the counting. Two lines.' },
    },
    {
      id: 'c2', type: 'coding', title: 'Parse Show Times', from: 'manager', day: 3, due: { day: 4, minute: 180 }, effort: 60, kind: 'individual', category: 'Coding',
      brief: '<p>Show schedules arrive as strings like <code>"2:30 PM"</code>. Write <code>parse_time(s)</code> returning <b>minutes since midnight</b>.</p><ul><li>AM/PM is case-insensitive</li><li><code>"12:00 AM"</code> is 0 (midnight) and <code>"12:15 PM"</code> is 735 (just after noon)</li></ul>',
      fnName: 'parse_time',
      starter: 'def parse_time(s):\n    # "2:30 PM" -> 870\n    pass\n',
      tests: [{ args: ['9:00 AM'], expected: 540 }, { args: ['2:30 PM'], expected: 870 }],
      hidden: [{ args: ['12:00 AM'], expected: 0 }, { args: ['12:15 PM'], expected: 735 }, { args: ['11:59 PM'], expected: 1439 }, { args: ['1:05 am'], expected: 65 }],
      hints: ['Split into "h:mm" and "AM/PM": s.strip().split().', 'hour % 12 turns 12 into 0. Then add 12 hours if it is PM.', 'Remember .upper() for the suffix.'],
      wiki: '<b>Time formats:</b> 12 AM is midnight, 12 PM is noon. The classic trick: <code>(hour % 12) + (12 if pm else 0)</code>.',
      peer: { who: 'leo', text: 'The 12 o\'clock cases got me TWICE. Use hour % 12.' },
    },
    {
      id: 'r1', type: 'review', title: 'Code Review: Shift Loader Script', from: 'mentor', day: 3, due: { day: 5, minute: 120 }, effort: 40, kind: 'individual', category: 'Code Review',
      brief: '<p>Review this script that loads staff shift hours from a CSV. Flag the real problems and leave a constructive comment.</p>',
      code: `def load_shifts(path, cache={}):
    if path in cache:
        return cache[path]
    f = open(path)
    rows = []
    for line in f.readlines():
        try:
            name, hours = line.split(",")
            rows.append((name, int(hours)))
        except:
            pass
    cache[path] = rows
    return rows`,
      issues: [
        { text: 'Mutable default argument: cache={} is shared across calls and keeps stale data forever.', real: true },
        { text: 'The file is never closed. Use a with-block.', real: true },
        { text: 'A bare except silently swallows every error, so bad rows disappear without a trace. Catch ValueError and log it.', real: true },
        { text: 'Names keep trailing whitespace/newlines and hours may include "\\n". Strip the values.', real: true },
        { text: 'Tuples are slower than lists, so rows should be lists.', real: false },
        { text: 'Python functions should never be longer than 5 lines.', real: false },
        { text: 'The script must use tabs, not spaces.', real: false },
      ],
      keywords: ['mutable', 'default', 'with', 'close', 'except', 'valueerror', 'log', 'strip'],
      hints: ['What happens the second time you call this function with a different file that has the same path?', 'Which errors are you actually expecting when parsing?'],
      wiki: '<b>Python gotchas:</b> default argument values are evaluated once. Use <code>None</code> and create inside. Prefer <code>with open(...)</code>.',
      peer: { who: 'marco', text: 'Bare except is how we lost half the schedule last spring. True story.' },
    },
    IS.T.standup({ day: 4, terms: ['script', 'parse', 'review', 'test', 'python', 'words', 'time'] }),
    {
      id: 'c3', type: 'coding', bugfix: true, title: 'Bug Fix: Average Rating Crash', from: 'manager', day: 4, due: { day: 5, minute: 180 }, effort: 45, kind: 'individual', category: 'Coding',
      brief: '<p>🐞 <b>OPS-311:</b> The guest-survey dashboard crashes on empty surveys and on missing answers. Fix <code>average_rating(ratings)</code>:</p><ul><li>ignore <code>None</code> and anything outside 1–5</li><li>return the average rounded to <b>2 decimals</b></li><li>no valid ratings → <code>0.0</code></li></ul>',
      fnName: 'average_rating',
      starter: 'def average_rating(ratings):\n    total = 0\n    for r in ratings:\n        total += r\n    return round(total / len(ratings))\n',
      tests: [{ args: [[5, 4, 3]], expected: 4.0 }, { args: [[5, null, 4]], expected: 4.5 }],
      hidden: [{ args: [[]], expected: 0.0 }, { args: [[9, 0, -1]], expected: 0.0 }, { args: [[1, 2]], expected: 1.5 }, { args: [[4, 4, 5]], expected: 4.33 }],
      hints: ['Filter first: valid = [r for r in ratings if r is not None and 1 <= r <= 5].', 'Guard the empty case before dividing.', 'round(x, 2) keeps two decimals.'],
      wiki: '<b>Python docs:</b> <code>round(number, ndigits)</code>. Dividing by <code>len([])</code> raises ZeroDivisionError.',
      peer: { who: 'amara', text: 'Division by zero AND None + int. Two bugs in one line, impressive.' },
    },
    IS.T.designDoc({ id: 'w-design1', title: 'Shift Scheduler Bot', day: 6, due: { day: 8, minute: 120 }, group: 'g1',
      brief: 'Your team builds a bot that assigns open shifts fairly. Write the design doc.',
      terms: ['shift', 'fair', 'capacity', 'round-robin', 'staff', 'schedule', 'test'], termsNeeded: 4 }),
    {
      id: 'c4', type: 'coding', title: 'Shift Scheduler: Round-Robin Assignment', from: 'manager', day: 6, due: { day: 8, minute: 180 }, effort: 75, kind: 'group', group: 'g1', category: 'Coding',
      brief: '<p>Your part of the Shift Scheduler Bot. Write <code>assign_shifts(staff, shifts)</code>. <code>staff</code> is a list of <code>{"name", "max_shifts"}</code>; <code>shifts</code> is a list of shift ids.</p><ul><li>go through shifts in order and assign <b>round-robin</b>: start with the person after whoever got the last shift</li><li>skip anyone at their <code>max_shifts</code></li><li>if nobody can take a shift, assign <code>None</code></li><li>return a dict <code>{shift_id: name_or_None}</code></li></ul>',
      fnName: 'assign_shifts',
      starter: 'def assign_shifts(staff, shifts):\n    # TODO\n    pass\n',
      tests: [
        { args: [[{ name: 'Ana', max_shifts: 1 }, { name: 'Ben', max_shifts: 2 }], ['s1', 's2', 's3']], expected: { s1: 'Ana', s2: 'Ben', s3: 'Ben' } },
        { args: [[], ['x']], expected: { x: null } },
      ],
      hidden: [
        { args: [[{ name: 'A', max_shifts: 2 }, { name: 'B', max_shifts: 2 }], ['a', 'b', 'c', 'd']], expected: { a: 'A', b: 'B', c: 'A', d: 'B' } },
        { args: [[{ name: 'A', max_shifts: 0 }, { name: 'B', max_shifts: 1 }], ['m', 'n']], expected: { m: 'B', n: null } },
        { args: [[{ name: 'A', max_shifts: 3 }], []], expected: {} },
      ],
      hints: ['Keep a pointer i into staff and a dict of how many shifts each person has.', 'For each shift, try up to len(staff) people starting at i; after assigning, set i to the next person.'],
      wiki: '<b>Scheduling 101:</b> round-robin spreads work evenly; capacity limits keep it fair.',
      peer: { who: 'amara', text: 'Track "used" per person and a rotating index. Modulo is your friend.' },
    },
    {
      id: 'c5', type: 'coding', title: 'Merge Maintenance Windows', from: 'mentor', day: 7, due: { day: 9, minute: 120 }, effort: 75, kind: 'individual', category: 'Coding',
      brief: '<p>Maintenance crews submit windows as <code>[start, end]</code> minutes. Write <code>merge_intervals(intervals)</code> that merges overlapping <b>or touching</b> windows and returns them sorted by start.</p>',
      fnName: 'merge_intervals',
      starter: 'def merge_intervals(intervals):\n    # TODO\n    pass\n',
      tests: [{ args: [[[1, 3], [2, 6], [8, 10]]], expected: [[1, 6], [8, 10]] }, { args: [[]], expected: [] }],
      hidden: [{ args: [[[5, 7], [1, 2]]], expected: [[1, 2], [5, 7]] }, { args: [[[1, 4], [4, 5]]], expected: [[1, 5]] }, { args: [[[1, 10], [2, 3]]], expected: [[1, 10]] }],
      hints: ['Sort by start first: sorted(intervals).', 'If the next start <= current end, extend the end with max().'],
      wiki: '<b>Algorithms:</b> merging intervals is sort + one linear pass.',
      peer: { who: 'sofia', text: 'Don\'t forget max() when extending. A window can be fully inside another.' },
    },
    {
      id: 'q2', type: 'quiz', title: 'Python Fundamentals Check', from: 'mentor', day: 8, due: { day: 8, minute: 180 }, effort: 20, kind: 'individual', category: 'Training',
      brief: '<p>Kenji wants to be sure everyone on the team shares the same Python basics.</p>',
      hints: ['Remember which types are mutable.', '`is` checks identity, not equality.'],
      wiki: '<b>Python docs:</b> lists and dicts are mutable; tuples and strings are not.',
      peer: { who: 'leo', text: 'I got the "is vs ==" one wrong the first time. Classic.' },
      questions: [
        { q: 'What does [x * 2 for x in range(3)] return?', options: ['[2, 4, 6]', '[0, 2, 4]', '[0, 1, 2]', '6'], answer: 1 },
        { q: 'd = {"a": 1}. What is d.get("b", 0)?', options: ['KeyError', 'None', '0', '"b"'], answer: 2 },
        { q: 'Which of these is mutable?', options: ['tuple', 'str', 'list', 'int'], answer: 2 },
        { q: 'What is the difference between `is` and `==`?', options: ['None', '`is` checks identity, `==` checks equality', '`==` is faster', '`is` only works on strings'], answer: 1 },
        { q: 'Which exception does int("abc") raise?', options: ['TypeError', 'KeyError', 'ValueError', 'IndexError'], answer: 2 },
      ],
    },
    IS.T.groupDemo({ day: 8, due: { day: 10, minute: 150 }, project: 'Shift Scheduler Bot', terms: ['shift', 'fair', 'staff', 'test', 'schedule', 'capacity'],
      failQ: 'What happens if the HR export is empty on a Monday morning?', failBest: 'The bot refuses to publish an empty schedule, alerts the ops lead, and keeps last week\'s schedule visible.' }),
    IS.T.midpoint({ terms: ['python', 'script'] }),
    {
      id: 'c6', type: 'coding', bugfix: true, title: 'Bug Fix: Batch Sender Drops Guests', from: 'manager', day: 12, due: { day: 13, minute: 180 }, effort: 60, kind: 'individual', category: 'Coding',
      brief: '<p>🐞 <b>OPS-402:</b> Guest notifications are sent in batches, but some guests never get messages. Fix <code>chunk(items, size)</code>: split <code>items</code> into lists of <code>size</code>, where the last list may be shorter. <code>size &lt;= 0</code> → <code>[]</code>.</p>',
      fnName: 'chunk',
      starter: 'def chunk(items, size):\n    return [items[i:i + size] for i in range(0, len(items) - size, size)]\n',
      tests: [{ args: [[1, 2, 3, 4, 5], 2], expected: [[1, 2], [3, 4], [5]] }, { args: [[], 3], expected: [] }],
      hidden: [{ args: [[1, 2, 3], 3], expected: [[1, 2, 3]] }, { args: [[1, 2], 5], expected: [[1, 2]] }, { args: [[1, 2, 3], 0], expected: [] }],
      hints: ['range(0, len(items), size) visits every chunk start, including the last partial one.', 'Guard size <= 0 first (range() with step 0 raises).'],
      wiki: '<b>Python docs:</b> slicing past the end is safe: <code>[1, 2][0:5] == [1, 2]</code>.',
      peer: { who: 'marco', text: 'The last partial batch is always the one that gets dropped. Always.' },
    },
    IS.T.email({ day: 13, due: { day: 13, minute: 180 }, to: 'Theo Park', toTitle: 'Show Producer', subject: 'Notification Delay',
      brief: 'Guest notifications for the evening parade will go out a day late because of the batching bug you found.',
      terms: ['notification', 'guest', 'fix', 'test', 'date', 'sorry|apolog'] }),
    {
      id: 'r2', type: 'review', title: 'Code Review: Lost & Found Notifier', from: 'intern3', day: 13, due: { day: 14, minute: 180 }, effort: 45, kind: 'individual', category: 'Code Review',
      brief: '<p>Sofia wants a review of a notifier that emails guests when their lost item is found.</p>',
      code: `import requests

API_TOKEN = "tok_live_58c1f9e2"

def notify(guest_email, item):
    msg = "Hi, we found your " + item
    url = "http://notify.internal/send?to=" + guest_email + "&msg=" + msg
    r = requests.post(url, headers={"Authorization": API_TOKEN}, verify=False)
    return r.status_code == 200`,
      issues: [
        { text: 'A live API token is hard-coded in source. Load it from a secret store and rotate it.', real: true },
        { text: 'verify=False disables TLS certificate checks, and the URL uses plain http.', real: true },
        { text: 'Email and message go into the query string unencoded (spaces, &, +) and leak guest PII into logs. Send them in the body.', real: true },
        { text: 'No timeout on requests.post, so a slow server hangs the job forever.', real: true },
        { text: 'The requests library is deprecated. Use urllib only.', real: false },
        { text: 'Function names must be camelCase in Python.', real: false },
        { text: 'String concatenation is not allowed in Python 3.', real: false },
      ],
      keywords: ['token', 'secret', 'verify', 'tls', 'https', 'encode', 'pii', 'timeout', 'body'],
      hints: ['There is a security issue, a privacy issue and a reliability issue.', 'What does verify=False turn off?'],
      wiki: '<b>Security policy:</b> never disable TLS verification. Personal data must not appear in URLs, since URLs end up in logs.',
      peer: { who: 'marco', text: 'That token is the real one. We should rotate it today.' },
    },
    {
      id: 'c7', type: 'coding', optional: true, title: '⭐ Stretch: Run-Length Encoding', from: 'mentor', day: 14, due: { day: 17, minute: 180 }, effort: 60, kind: 'individual', category: 'Coding',
      brief: '<p><b>Optional stretch.</b> Ride logs compress repeated states. Write <code>rle_encode(s)</code>: <code>"aaabcc"</code> → <code>"a3b1c2"</code>. Empty string → <code>""</code>.</p>',
      fnName: 'rle_encode',
      starter: 'def rle_encode(s):\n    # "aaabcc" -> "a3b1c2"\n    pass\n',
      tests: [{ args: ['aaabcc'], expected: 'a3b1c2' }, { args: [''], expected: '' }],
      hidden: [{ args: ['a'], expected: 'a1' }, { args: ['zzzzzzzzzzzz'], expected: 'z12' }, { args: ['abab'], expected: 'a1b1a1b1' }],
      hints: ['itertools.groupby groups consecutive equal characters.', 'Or loop and keep the current char and a count.'],
      wiki: '<b>Python docs:</b> <code>itertools.groupby(s)</code> yields (key, group) for each run.',
      peer: { who: 'sofia', text: 'groupby makes this a one-liner. A readable one, even.' },
    },
    IS.T.designDoc({ id: 'w-design2', title: 'Lost & Found Matcher', day: 16, due: { day: 17, minute: 180 }, group: 'g2', minWords: 180, metrics: true,
      brief: 'The capstone: match guests\' lost-item reports to found items, notify guests, and give the team a daily summary. Privacy matters.',
      terms: ['lost', 'found', 'match', 'privacy', 'guest', 'notify', 'color', 'fake data'], termsNeeded: 5 }),
    {
      id: 'c8', type: 'coding', title: 'Capstone: Match Lost Items', from: 'manager', day: 16, due: { day: 19, minute: 180 }, effort: 105, kind: 'group', group: 'g2', category: 'Coding',
      brief: '<p>Core of the matcher. <code>lost</code> and <code>found</code> are lists of <code>{"id", "item", "color"}</code>. Write <code>match_items(lost, found)</code> returning a list of <code>[lost_id, found_id]</code> pairs:</p><ul><li>a match needs the same <code>item</code> and <code>color</code>, <b>case-insensitive</b> and ignoring surrounding spaces</li><li>process lost reports in order; each takes the <b>first unused</b> matching found item</li><li>each found item can be used only once</li></ul>',
      fnName: 'match_items',
      starter: 'def match_items(lost, found):\n    # TODO\n    pass\n',
      tests: [
        { args: [[{ id: 'L1', item: 'Hat', color: 'Red' }], [{ id: 'F1', item: 'hat', color: 'red' }]], expected: [['L1', 'F1']] },
        { args: [[], [{ id: 'F1', item: 'hat', color: 'red' }]], expected: [] },
      ],
      hidden: [
        { args: [[{ id: 'L1', item: 'phone', color: 'black' }, { id: 'L2', item: 'phone', color: 'black' }], [{ id: 'F9', item: 'Phone ', color: 'Black' }]], expected: [['L1', 'F9']] },
        { args: [[{ id: 'L1', item: 'bag', color: 'green' }], [{ id: 'F1', item: 'bag', color: 'tan' }, { id: 'F2', item: 'bag', color: 'GREEN' }]], expected: [['L1', 'F2']] },
        { args: [[{ id: 'L1', item: 'x', color: 'y' }, { id: 'L2', item: 'a', color: 'b' }], [{ id: 'F1', item: 'a', color: 'b' }, { id: 'F2', item: 'x', color: 'y' }]], expected: [['L1', 'F2'], ['L2', 'F1']] },
      ],
      hints: ['Normalize with .strip().lower() on both fields.', 'Keep a set of used found ids.', 'Loop lost in order; for each, loop found in order and take the first unused match.'],
      wiki: '<b>Matching:</b> greedy first-match is simple and predictable. Normalizing text is half the battle.',
      peer: { who: 'sofia', text: 'I normalized with a helper key(x) = (x["item"].strip().lower(), x["color"].strip().lower()).' },
    },
    {
      id: 'c9', type: 'coding', title: 'Top Lost Categories', from: 'intern3', day: 18, due: { day: 20, minute: 180 }, effort: 75, kind: 'individual', category: 'Coding',
      brief: '<p>For the daily summary: write <code>top_categories(items, k)</code>. <code>items</code> is a list of category names. Return the <code>k</code> most common as <code>[[name, count], ...]</code>, sorted by count (highest first), ties by name alphabetically. Names are case-insensitive (return them lowercase).</p>',
      fnName: 'top_categories',
      starter: 'def top_categories(items, k):\n    # TODO\n    pass\n',
      tests: [{ args: [['hat', 'phone', 'hat', 'Bag'], 2], expected: [['hat', 2], ['bag', 1]] }, { args: [[], 3], expected: [] }],
      hidden: [{ args: [['A', 'a', 'b'], 5], expected: [['a', 2], ['b', 1]] }, { args: [['z', 'y', 'x'], 2], expected: [['x', 1], ['y', 1]] }, { args: [['p', 'p', 'q'], 0], expected: [] }],
      hints: ['Count lowercase names with a dict or Counter.', 'sorted(counts.items(), key=lambda kv: (-kv[1], kv[0]))[:k]', 'Convert the tuples to lists.'],
      wiki: '<b>Python docs:</b> sort by several keys by returning a tuple from the key function. Negate numbers for descending order.',
      peer: { who: 'amara', text: 'The (-count, name) sort key trick is my favorite thing I learned this summer.' },
    },
    {
      id: 'c10', type: 'coding', urgent: true, bugfix: true, title: '🚨 URGENT: Kiosk Crashes on Badge IDs', from: 'manager', day: 22, due: { day: 22, minute: 120 }, effort: 40, kind: 'individual', category: 'Coding',
      brief: '<p>🚨 <b>SEV-2:</b> Cast members with badge IDs like <code>"CM-00123"</code> can\'t check in: the kiosk crashes. Fix <code>parse_badge(badge)</code> <b>by 11:00 AM</b>:</p><ul><li>return the numeric part as an int: <code>"CM-00123"</code> → 123, <code>"00123"</code> → 123, <code>"cm00042"</code> → 42</li><li>ignore surrounding whitespace</li><li>no digits at all → <code>None</code></li></ul>',
      fnName: 'parse_badge',
      starter: 'def parse_badge(badge):\n    return int(badge)\n',
      tests: [{ args: ['00123'], expected: 123 }, { args: ['CM-00123'], expected: 123 }],
      hidden: [{ args: ['cm00042'], expected: 42 }, { args: ['  77 '], expected: 77 }, { args: ['ABC'], expected: null }, { args: [''], expected: null }],
      hints: ['Keep only the digits: "".join(ch for ch in badge if ch.isdigit()).', 'If that string is empty, return None; otherwise int() it.'],
      wiki: '<b>Incident runbook:</b> mitigate first. The smallest safe fix wins. Write the postmortem within 24h.',
      peer: { who: 'marco', text: 'There\'s a line of 40 cast members at the kiosk right now. No pressure 😬' },
    },
    IS.T.postmortem({ title: 'Kiosk Check-in Crash', day: 22, due: { day: 23, minute: 180 }, terms: ['badge', 'letters|digit', 'int', 'crash', 'test', 'monitor|alert', 'kiosk'] }),
    {
      id: 'c11', type: 'coding', title: 'Capstone: Daily Summary Report', from: 'intern4', day: 21, due: { day: 23, minute: 180 }, effort: 90, kind: 'group', group: 'g2', category: 'Coding',
      brief: '<p>Write <code>daily_summary(events)</code>. Each event is <code>{"type": "lost" | "found" | "returned", "zone": str}</code>. Return a dict with totals per type (all three keys, even if 0) and <code>"busiest_zone"</code>: the zone with the most events (ties → alphabetical; no events → <code>None</code>).</p>',
      fnName: 'daily_summary',
      starter: 'def daily_summary(events):\n    # TODO\n    pass\n',
      tests: [
        { args: [[{ type: 'lost', zone: 'Frontier' }, { type: 'found', zone: 'Frontier' }, { type: 'lost', zone: 'Hub' }]], expected: { lost: 2, found: 1, returned: 0, busiest_zone: 'Frontier' } },
        { args: [[]], expected: { lost: 0, found: 0, returned: 0, busiest_zone: null } },
      ],
      hidden: [
        { args: [[{ type: 'returned', zone: 'B' }, { type: 'returned', zone: 'A' }]], expected: { lost: 0, found: 0, returned: 2, busiest_zone: 'A' } },
        { args: [[{ type: 'found', zone: 'Z' }, { type: 'found', zone: 'Z' }, { type: 'lost', zone: 'A' }]], expected: { lost: 1, found: 2, returned: 0, busiest_zone: 'Z' } },
      ],
      hints: ['Start with {"lost": 0, "found": 0, "returned": 0}.', 'Count zones in a second dict, then pick min by (-count, name).'],
      wiki: '<b>Reporting:</b> always include zero counts. A missing key looks like a bug to the reader.',
      peer: { who: 'marco', text: 'Ops leads read this at 7 AM with coffee. Make it boringly correct.' },
    },
    {
      id: 'c12', type: 'coding', title: 'Capstone: Anonymize Guest Records', from: 'mentor', day: 24, due: { day: 26, minute: 180 }, effort: 75, kind: 'group', group: 'g2', category: 'Coding',
      brief: '<p>Before the summary is shared, personal data must be masked. Write <code>anonymize(record)</code> for a dict with <code>name</code>, <code>email</code>, <code>phone</code> (any may be missing). Return a <b>new</b> dict:</p><ul><li><code>name</code> → first letter + <code>"***"</code> (<code>"Ada"</code> → <code>"A***"</code>)</li><li><code>email</code> → keep the domain: <code>"ada@mail.com"</code> → <code>"***@mail.com"</code></li><li><code>phone</code> → keep last 4 digits: <code>"407-555-0199"</code> → <code>"***0199"</code></li><li>copy other keys unchanged; don\'t modify the input</li></ul>',
      fnName: 'anonymize',
      starter: 'def anonymize(record):\n    # TODO\n    pass\n',
      tests: [{ args: [{ name: 'Ada', email: 'ada@mail.com', phone: '407-555-0199', item: 'hat' }], expected: { name: 'A***', email: '***@mail.com', phone: '***0199', item: 'hat' } }],
      hidden: [{ args: [{ item: 'bag' }], expected: { item: 'bag' } }, { args: [{ name: 'Bo', phone: '12' }], expected: { name: 'B***', phone: '***12' } }, { args: [{ email: 'x@y.org' }], expected: { email: '***@y.org' } }],
      hints: ['Start with out = dict(record) so you never touch the input.', 'email.split("@")[-1] is the domain; keep only digits of the phone and take [-4:].'],
      wiki: '<b>Privacy:</b> data minimization means sharing only what the reader needs.',
      peer: { who: 'sofia', text: 'Phone formats are chaos. Strip to digits first.' },
    },
    IS.T.status({ day: 25, terms: ['capstone', 'kiosk', 'postmortem', 'match', 'privacy', 'demo', 'test'] }),
    IS.T.readme({ day: 26, due: { day: 28, minute: 180 }, project: 'Lost & Found Matcher', terms: ['match_items', 'anonymize', 'daily_summary', 'privacy', 'pytest|test', 'python'] }),
    IS.T.finalPres({ project: 'Lost & Found Matcher', terms: ['lost', 'found', 'match', 'privacy', 'guest', 'notify', 'summary'], questions: [
      { who: 'harriet', q: 'How does this change a guest\'s day?', options: [
        { text: 'It makes the database faster.', pts: 3 },
        { text: 'A family who lost a stuffed animal gets a message within minutes instead of waiting in a line at guest services. That turns a bad moment into a good story.', pts: 10 },
        { text: 'Guests won\'t notice.', pts: 0 },
        { text: 'It\'s mostly for cast members.', pts: 4 }] },
      { who: 'harriet', q: 'What about guest privacy?', options: [
        { text: 'We anonymize names, emails and phones before anything is shared, secrets live in a vault, and data is deleted after 30 days.', pts: 10 },
        { text: 'Only engineers can see the data.', pts: 4 },
        { text: 'Privacy is handled by legal.', pts: 0 },
        { text: 'We\'ll add it later.', pts: 1 }] },
      { who: 'manager', q: 'What\'s the biggest risk?', options: [
        { text: 'False matches. We require item and color to match and a cast member confirms before contacting a guest.', pts: 10 },
        { text: 'None really.', pts: 0 },
        { text: 'The servers.', pts: 3 },
        { text: 'Marco\'s data.', pts: 1 }] },
      { who: 'mentor', q: 'How would it scale to every park?', options: [
        { text: 'Index found items by (item, color) in a dict so matching is O(1) per report, and run one job per park.', pts: 10 },
        { text: 'Bigger servers.', pts: 2 },
        { text: 'It should be fine.', pts: 2 },
        { text: 'Rewrite it in C.', pts: 3 }] },
    ] }),
    IS.T.selfEval({ day: 30, due: { day: 31, minute: 180 }, terms: ['python', 'capstone', 'kiosk', 'team', 'test', 'deadline', 'privacy'] }),
  ],
  interview: {
    coding: [
      { id: 'i-py1', title: 'Anagram Check', fnName: 'is_anagram',
        brief: '<p>Write <code>is_anagram(a, b)</code>: True if the two strings use exactly the same letters, ignoring spaces and case. <code>"Listen", "Silent"</code> → True.</p>',
        starter: 'def is_anagram(a, b):\n    pass\n',
        tests: [{ args: ['Listen', 'Silent'], expected: true }, { args: ['abc', 'abd'], expected: false }],
        hidden: [{ args: ['Dormitory', 'dirty room'], expected: true }, { args: ['aab', 'ab'], expected: false }, { args: ['', ''], expected: true }] },
      { id: 'i-py2', title: 'First Unique Character', fnName: 'first_unique',
        brief: '<p>Write <code>first_unique(s)</code>: the index of the first character that appears exactly once, or -1. <code>"leetcode"</code> → 0, <code>"aabb"</code> → -1.</p>',
        starter: 'def first_unique(s):\n    pass\n',
        tests: [{ args: ['leetcode'], expected: 0 }, { args: ['aabb'], expected: -1 }],
        hidden: [{ args: ['loveleetcode'], expected: 2 }, { args: [''], expected: -1 }, { args: ['z'], expected: 0 }] },
      { id: 'i-py3', title: 'Running Total', fnName: 'running_total',
        brief: '<p>Write <code>running_total(nums)</code> returning the list of prefix sums. <code>[1, 2, 3]</code> → <code>[1, 3, 6]</code>.</p>',
        starter: 'def running_total(nums):\n    pass\n',
        tests: [{ args: [[1, 2, 3]], expected: [1, 3, 6] }, { args: [[]], expected: [] }],
        hidden: [{ args: [[5]], expected: [5] }, { args: [[-1, 1, -1]], expected: [-1, 0, -1] }] },
    ],
    concepts: [
      { q: 'What does [x * 2 for x in range(3)] return?', options: ['[2, 4, 6]', '[0, 2, 4]', '[0, 1, 2]', '6'], answer: 1 },
      { q: 'What is d.get("missing", 0) for d = {}?', options: ['KeyError', 'None', '0', 'False'], answer: 2 },
      { q: 'Which type is immutable?', options: ['list', 'dict', 'set', 'tuple'], answer: 3 },
      { q: 'What does s[::-1] do to a string s?', options: ['Removes the last char', 'Reverses it', 'Copies the first char', 'Error'], answer: 1 },
      { q: 'Why is `def f(x, cache={})` risky?', options: ['It is a syntax error', 'The default dict is shared across calls', 'Dicts can\'t be arguments', 'It\'s slow'], answer: 1 },
      { q: 'What does a `with open(path) as f:` block guarantee?', options: ['Faster reads', 'The file is closed afterwards, even on errors', 'The file is created', 'Nothing'], answer: 1 },
      { q: 'What is the average time complexity of `key in some_dict`?', options: ['O(1)', 'O(log n)', 'O(n)', 'O(n²)'], answer: 0 },
      { q: 'Which exception does int("abc") raise?', options: ['TypeError', 'ValueError', 'KeyError', 'NameError'], answer: 1 },
    ],
  },
  training: {
    lessons: [
      { title: 'Pythonic basics', html: '<pre>squares = [n * n for n in nums if n > 0]\ncounts = {}\nfor w in words:\n    counts[w] = counts.get(w, 0) + 1\nfor i, ch in enumerate(s):\n    ...</pre><p>Comprehensions, <code>dict.get</code> and <code>enumerate</code> cover most interview loops.</p>' },
      { title: 'Strings', html: '<pre>s.strip().lower().split()   # clean + tokenize\ns[::-1]                     # reverse\n"".join(sorted(s))          # canonical form for anagrams\nch.isalnum(), ch.isdigit()</pre>' },
      { title: 'Counting & sets', html: '<p><code>collections.Counter(s)</code> counts characters. Two strings are anagrams if their Counters are equal. A <code>set</code> answers "have I seen this?" in O(1).</p>' },
    ],
  },
});
