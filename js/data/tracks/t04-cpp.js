// Internship 4: C++, Figure Animation Controls (animatronics firmware).
// Tasks use C-style C++ as found on microcontrollers: fixed arrays, no heap, no STL.
IS.addCharacters({
  ruth: {
    name: 'Ruth Adeyemi', title: 'Engineering Manager, Figure Animation Controls', role: 'Manager',
    bio: 'Ruth has brought more than forty animatronic figures to life. Safety reviews are sacred to her, and so are show dates.',
    look: { skin: '#6b4029', hair: 'braids', hairColor: '#1c1714', eyes: '#3b2618', top: 'labcoat', bottom: 'pants', bottomColor: '#2b2622', shoes: 'boots', accessory: 'glasses' },
    chat: ['Firmware bugs don\'t crash. They move. That\'s why we test so hard.', 'A figure that fails safe is better than a figure that fails fancy.', 'Always ask: what happens if this value is garbage?'],
  },
  viktor: {
    name: 'Viktor Sokolov', title: 'Principal Firmware Engineer (your mentor)', role: 'Mentor',
    bio: 'Viktor has written C for robots, rockets and one very famous talking bird. Counts bytes for fun.',
    look: { skin: '#f1d0b5', hair: 'short', hairColor: '#a3a09a', eyes: '#6b6f5c', facial: 'beard', top: 'sweater', topColor: '#5a3e2b', bottom: 'pants', bottomColor: '#4b5a3a', shoes: 'boots', build: 'broad' },
    chat: ['On a microcontroller, every array has a size and every size is a promise.', 'Never block the control loop. Ever.', 'Undefined behavior is the scariest phrase in engineering.'],
  },
  lily: {
    name: 'Lily Chen', title: 'Mechatronics Intern', role: 'Intern', reliability: 0.9,
    bio: 'Calibrates servos with the patience of a watchmaker. Meticulous notes, great teammate.',
    look: { skin: '#f1d0b5', hair: 'ponytail', hairColor: '#1c1714', eyes: '#3b2618', top: 'hivis', bottom: 'pants', bottomColor: '#5a4636', shoes: 'boots', hat: 'hardhat' },
    chat: ['Servo 4 is off by two degrees. It\'s bothering me on a spiritual level.', 'Measure twice, flash once.'],
  },
  dante: {
    name: 'Dante Morales', title: 'Embedded Software Intern', role: 'Intern', reliability: 0.6,
    bio: 'Can bit-twiddle anything. Can\'t estimate anything.',
    look: { skin: '#c68b5f', hair: 'curly', hairColor: '#1c1714', eyes: '#3b2618', facial: 'mustache', top: 'tee', topColor: '#2b2622', bottom: 'pants', bottomColor: '#8a6d4b', shoes: 'sneakers', accessory: 'headphones' },
    chat: ['I rewrote the loop with bit shifts. It\'s 3 nanoseconds faster and unreadable.', 'The deadline is a suggestion. (It is not a suggestion.)'],
  },
  mei: {
    name: 'Mei Nakamura', title: 'Robotics Intern', role: 'Intern', reliability: 0.95,
    bio: 'Robotics PhD student. Writes firmware like it\'s going to space.',
    look: { skin: '#e7bf9c', hair: 'bob', hairColor: '#2f2019', eyes: '#3b2618', top: 'labcoat', bottom: 'pants', bottomColor: '#2b2622', shoes: 'sneakers', accessory: 'glasses' },
    chat: ['Every input is hostile until proven otherwise.', 'I wrote a test that runs the figure through 10,000 random poses overnight.'],
  },
  arjun: {
    name: 'Arjun Mehta', title: 'Electrical Engineering Intern', role: 'Intern', reliability: 0.7,
    bio: 'Solders like a surgeon. Hardware deliveries are his nemesis.',
    look: { skin: '#a8704a', hair: 'sidepart', hairColor: '#1c1714', eyes: '#3b2618', facial: 'stubble', top: 'button', topColor: '#6f8f5e', bottom: 'pants', bottomColor: '#2b2622', shoes: 'loafers' },
    chat: ['If it smells like burning, it probably is.', 'Customs has our boards. Customs has my heart.'],
  },
});

IS.registerTrack({
  id: 'cpp', n: 4, icon: '🤖', lang: 'cpp', langLabel: 'C++', rate: 30,
  title: 'Firmware Engineering Intern', team: 'Figure Animation Controls',
  blurb: 'Write embedded C++ for animatronic figures: servo limits, sensor filtering, checksums, fault codes and watchdogs. No heap, no STL, fixed arrays, like real microcontroller code.',
  skills: ['C++ (embedded style)', 'Arrays & bounds', 'Bit manipulation', 'Safety-critical thinking'],
  channel: 'figure-controls', lab: { name: 'Figure Animation Lab', art: 'robot' },
  cast: { manager: 'ruth', mentor: 'viktor', interns: ['lily', 'dante', 'mei', 'arjun'] },
  groups: { g1: { name: 'Servo Calibration Tool', members: ['lily', 'dante'] }, g2: { name: 'Animatronic Safety Monitor', members: ['mei', 'arjun'] } },
  incident: 'A figure\'s arm is twitching mid-show: a sensor glitch is passing straight through the filter.',
  scenario: { conflictA: 'a live demo on the real figure', conflictB: 'the board simulator on screen', delay: 'the new controller boards are stuck in customs', delayFix: 'a software simulator for the board' },
  tasks: [
    {
      id: 'q1', type: 'quiz', title: 'Figure Lab Safety', from: 'rosa', day: 1, due: { day: 1, minute: 180 }, effort: 20, kind: 'individual', category: 'Training',
      brief: '<p>Animatronic figures are heavy, fast and strong. Pass the lab safety check (80%+) before entering.</p>',
      hints: ['E-stop and lockout/tagout are the heart of lab safety.', 'If you are unsure, stop and ask.'],
      wiki: '<b>Lab rules:</b> know the E-stop locations, lock out power before touching mechanisms, stay out of the motion envelope during tests.',
      peer: { who: 'lily', text: 'Lockout/tagout: your lock, your key, your safety.' },
      questions: [
        { q: 'Before reaching into a figure to adjust a linkage, you must:', options: ['Ask someone to hold still.', 'Lock out and tag out its power so it can\'t move.', 'Turn the speed down.', 'Wear gloves.'], answer: 1 },
        { q: 'A figure moves unexpectedly during a test. First action?', options: ['Grab the arm.', 'Hit the emergency stop.', 'Film it.', 'Reboot the laptop.'], answer: 1 },
        { q: 'The "motion envelope" is:', options: ['A shipping container', 'The space the figure can reach while moving: stay out during tests', 'A type of servo', 'A config file'], answer: 1 },
        { q: 'You notice a frayed cable on a figure\'s arm. You:', options: ['Tape it.', 'Tag it and report it to the lab lead.', 'Ignore it until the show ends.', 'Pull it out.'], answer: 1 },
        { q: 'New firmware should be tested on the real figure:', options: ['Right before the show.', 'After simulator tests, at reduced speed, with the E-stop in reach.', 'At full speed first.', 'Never.'], answer: 1 },
      ],
    },
    IS.T.intro({ channel: 'figure-controls', team: 'Figure Animation Controls', teamKey: 'firmware' }),
    {
      id: 'c1', type: 'coding', title: 'Clamp Servo Angles', from: 'mentor', day: 2, due: { day: 3, minute: 180 }, effort: 60, kind: 'individual', category: 'Coding', fnName: 'clampAngle',
      brief: '<p>Servos must never be commanded past their mechanical limits. Write <code>int clampAngle(int angle, int lo, int hi)</code> that returns <code>angle</code> limited to the range [lo, hi].</p>',
      starter: '// Keep commanded angles inside the safe range.\nint clampAngle(int angle, int lo, int hi) {\n    // TODO\n    return angle;\n}\n',
      tests: [{ call: 'clampAngle(90, 0, 180)', expected: '90' }, { call: 'clampAngle(200, 0, 180)', expected: '180' }],
      hidden: [{ call: 'clampAngle(-5, 0, 180)', expected: '0' }, { call: 'clampAngle(20, 30, 150)', expected: '30' }, { call: 'clampAngle(150, 30, 150)', expected: '150' }],
      hints: ['if (angle < lo) return lo; if (angle > hi) return hi; return angle;'],
      wiki: '<b>Figure safety:</b> soft limits in software always sit inside the hard mechanical stops.',
      peer: { who: 'lily', text: 'Test the exact edges too. 150 should stay 150.' },
    },
    {
      id: 'c2', type: 'coding', title: 'Count Fault Codes', from: 'manager', day: 3, due: { day: 4, minute: 180 }, effort: 60, kind: 'individual', category: 'Coding', fnName: 'countFaults',
      brief: '<p>Each figure reports an array of status codes (0 = OK). Write <code>int countFaults(const int codes[], int n)</code> returning how many of the first <code>n</code> codes are non-zero.</p>',
      starter: 'int countFaults(const int codes[], int n) {\n    // TODO\n    return 0;\n}\n',
      tests: [{ setup: 'int c[4] = {0, 3, 0, 7};', call: 'countFaults(c, 4)', expected: '2' }, { setup: 'int c[2] = {0, 0};', call: 'countFaults(c, 2)', expected: '0' }],
      hidden: [{ setup: 'int c[1]; c[0] = 5;', call: 'countFaults(c, 1)', expected: '1' }, { setup: 'int c[3] = {9, 9, 9};', call: 'countFaults(c, 0)', expected: '0' }, { setup: 'int c[3] = {-1, 0, 2};', call: 'countFaults(c, 3)', expected: '2' }],
      hints: ['Loop i from 0 to n − 1 and count codes[i] != 0.'],
      wiki: '<b>C arrays:</b> an array parameter doesn\'t know its length. Always pass n and never read past it.',
      peer: { who: 'mei', text: 'Negative codes are faults too. Anything non-zero.' },
    },
    {
      id: 'r1', type: 'review', title: 'Code Review: Sensor Averaging Module', from: 'mentor', day: 3, due: { day: 5, minute: 120 }, effort: 40, kind: 'individual', category: 'Code Review',
      brief: '<p>Review this module that averages recent sensor readings on the figure controller.</p>',
      code: `int readings[10];
int count = 0;

void addReading(int value) {
    readings[count] = value;
    count++;
}

int average() {
    int sum = 0;
    for (int i = 0; i <= count; i++) sum += readings[i];
    return sum / count;
}`,
      issues: [
        { text: 'Buffer overflow: after 10 readings, addReading writes past the end of the array.', real: true },
        { text: 'Off-by-one: the loop uses <= count and reads one element past the valid data.', real: true },
        { text: 'Division by zero when average() is called before any reading.', real: true },
        { text: 'Global mutable state isn\'t safe if an interrupt adds readings while average() runs.', real: true },
        { text: 'Should use printf instead of cout.', real: false },
        { text: 'int is too small to store sensor values.', real: false },
        { text: 'All functions must be declared inline on microcontrollers.', real: false },
      ],
      keywords: ['overflow', 'bound', 'off-by-one', '<=', 'zero', 'interrupt', 'global', 'ring buffer'],
      hints: ['What happens on the 11th reading?', 'Count the loop iterations when count is 3.'],
      wiki: '<b>Embedded C:</b> use a ring buffer with an index modulo the size; guard division; protect shared state from ISRs.',
      peer: { who: 'dante', text: 'Four bugs in eleven lines. That\'s a record even for me.' },
    },
    IS.T.standup({ day: 4, terms: ['servo', 'firmware', 'array', 'review', 'test', 'fault'] }),
    {
      id: 'c3', type: 'coding', bugfix: true, title: 'Bug Fix: Peak Sensor Reading', from: 'manager', day: 4, due: { day: 5, minute: 180 }, effort: 45, kind: 'individual', category: 'Coding', fnName: 'maxReading',
      brief: '<p>🐞 <b>FIG-217:</b> The peak-load monitor reports 0 when all readings are negative, and sometimes reads garbage memory. Fix <code>int maxReading(const int a[], int n)</code> to return the largest of the first <code>n</code> values. If <code>n &lt;= 0</code>, return 0.</p>',
      starter: 'int maxReading(const int a[], int n) {\n    int best = 0;\n    for (int i = 1; i <= n; i++) {\n        if (a[i] > best) best = a[i];\n    }\n    return best;\n}\n',
      tests: [{ setup: 'int a[3] = {3, 8, 1};', call: 'maxReading(a, 3)', expected: '8' }, { setup: 'int a[3] = {-5, -2, -9};', call: 'maxReading(a, 3)', expected: '-2' }],
      hidden: [{ setup: 'int a[1]; a[0] = 42;', call: 'maxReading(a, 1)', expected: '42' }, { setup: 'int a[1]; a[0] = 1;', call: 'maxReading(a, 0)', expected: '0' }, { setup: 'int a[3] = {7, 7, 2};', call: 'maxReading(a, 3)', expected: '7' }],
      hints: ['Start best at a[0], not 0.', 'Valid indexes are 0 … n − 1, so loop with i < n.', 'Handle n <= 0 before touching a[0].'],
      wiki: '<b>C:</b> reading a[n] is undefined behavior. It may "work" on your desk and fail on stage.',
      peer: { who: 'arjun', text: 'Negative loads happen when the arm is pulling against gravity.' },
    },
    IS.T.designDoc({ id: 'w-design1', title: 'Servo Calibration Tool', day: 6, due: { day: 8, minute: 120 }, group: 'g1',
      brief: 'Your team builds a tool that moves each servo smoothly between calibration poses and records its true limits.',
      terms: ['servo', 'limit', 'clamp', 'interpolat', 'safety', 'e-stop', 'test'], termsNeeded: 4 }),
    {
      id: 'c4', type: 'coding', title: 'Calibration Tool: Smooth Pose Blend', from: 'manager', day: 6, due: { day: 8, minute: 180 }, effort: 75, kind: 'group', group: 'g1', category: 'Coding', fnName: 'lerpAngle',
      brief: '<p>Write <code>double lerpAngle(double from, double to, double t)</code> that blends between two poses: <code>from + (to − from) × t</code>, with <code>t</code> clamped to [0, 1] so the figure never overshoots.</p>',
      starter: 'double lerpAngle(double from, double to, double t) {\n    // TODO\n    return from;\n}\n',
      tests: [{ call: 'lerpAngle(0, 90, 0.5)', expected: '45' }, { call: 'lerpAngle(10, 20, 1.5)', expected: '20' }],
      hidden: [{ call: 'lerpAngle(10, 20, -1)', expected: '10' }, { call: 'lerpAngle(0, 100, 0.25)', expected: '25' }, { call: 'lerpAngle(90, 30, 0.5)', expected: '60' }],
      hints: ['Clamp t first: if (t < 0) t = 0; if (t > 1) t = 1;', 'Then return from + (to - from) * t;'],
      wiki: '<b>Animation:</b> linear interpolation (lerp) is the building block of every motion profile.',
      peer: { who: 'dante', text: 'Overshoot on a real figure = a very expensive noise. Clamp t!' },
    },
    {
      id: 'c5', type: 'coding', title: 'Serial Packet Checksum', from: 'mentor', day: 7, due: { day: 9, minute: 120 }, effort: 75, kind: 'individual', category: 'Coding', fnName: 'checksum',
      brief: '<p>Commands reach figures over a serial bus. Write <code>int checksum(const char msg[])</code> returning the sum of the character codes of the null-terminated string, modulo 256.</p>',
      starter: 'int checksum(const char msg[]) {\n    // TODO\n    return 0;\n}\n',
      tests: [{ call: 'checksum("AB")', expected: '131' }, { call: 'checksum("")', expected: '0' }],
      hidden: [{ call: 'checksum("Hello")', expected: '244' }, { call: 'checksum("~~~")', expected: '122' }],
      hints: ['Loop until msg[i] == \'\\0\'.', 'Add (int)msg[i] each time, then return total % 256.'],
      wiki: '<b>Protocols:</b> checksums catch corrupted bytes on noisy lines. Real systems often use CRCs.',
      peer: { who: 'mei', text: 'C strings end at the zero byte. Don\'t use a fixed length.' },
    },
    {
      id: 'q2', type: 'quiz', title: 'C/C++ Fundamentals', from: 'mentor', day: 8, due: { day: 8, minute: 180 }, effort: 20, kind: 'individual', category: 'Training',
      brief: '<p>Viktor\'s quick check on the C++ you\'ll use every day.</p>',
      hints: ['Integer division truncates.', 'Array indexes start at 0.'],
      wiki: '<b>C++ basics:</b> fixed-size arrays, integer math, references and const.',
      peer: { who: 'lily', text: 'The integer division one gets everybody.' },
      questions: [
        { q: 'In C++, what is 7 / 2 (both ints)?', options: ['3.5', '3', '4', 'Error'], answer: 1 },
        { q: 'Valid indexes for int a[5] are:', options: ['1 to 5', '0 to 4', '0 to 5', 'Any'], answer: 1 },
        { q: 'Reading past the end of an array is:', options: ['An exception', 'Undefined behavior', 'Always 0', 'A compile error'], answer: 1 },
        { q: 'What does `const int limit` mean?', options: ['limit can\'t be changed after initialization', 'limit is global', 'limit is unsigned', 'limit is a pointer'], answer: 0 },
        { q: 'What does `mask & (1 << 3)` test?', options: ['Whether mask is 3', 'Whether bit 3 is set', 'Multiplies by 8', 'Shifts mask'], answer: 1 },
      ],
    },
    IS.T.groupDemo({ day: 8, due: { day: 10, minute: 150 }, project: 'Servo Calibration Tool', terms: ['servo', 'limit', 'clamp', 'safety', 'test', 'pose'],
      failQ: 'What happens if a calibration command is corrupted on the bus?', failBest: 'The checksum rejects it, the servo holds its last safe pose, and the tool logs a fault for the technician.' }),
    IS.T.midpoint({ terms: ['firmware', 'c++'] }),
    {
      id: 'c6', type: 'coding', title: 'Smooth a Motion Curve', from: 'mentor', day: 11, due: { day: 12, minute: 180 }, effort: 90, kind: 'individual', category: 'Coding', fnName: 'smooth',
      brief: '<p>Write <code>void smooth(const int in[], int out[], int n)</code>. Each <code>out[i]</code> is the integer average of <code>in[i−1]</code>, <code>in[i]</code>, <code>in[i+1]</code>. The first and last values are copied unchanged. If n is 1 or 2, copy everything.</p>',
      starter: 'void smooth(const int in[], int out[], int n) {\n    // TODO\n}\n',
      tests: [{ main: 'int a[5] = {10, 40, 10, 40, 10}; int o[5]; smooth(a, o, 5); for (int i = 0; i < 5; i++) cout << o[i] << " "; cout << endl;', expected: '10 20 30 20 10' }],
      hidden: [{ main: 'int a[2] = {3, 9}; int o[2]; smooth(a, o, 2); cout << o[0] << " " << o[1] << endl;', expected: '3 9' }, { main: 'int a[4] = {0, 3, 6, 9}; int o[4]; smooth(a, o, 4); for (int i = 0; i < 4; i++) cout << o[i] << " "; cout << endl;', expected: '0 3 6 9' }],
      hints: ['Copy the first and last elements before the loop.', 'For i from 1 to n − 2: out[i] = (in[i-1] + in[i] + in[i+1]) / 3;'],
      wiki: '<b>Motion control:</b> smoothing removes jerk, which matters for mechanical wear and realism.',
      peer: { who: 'lily', text: 'Guard n < 3 so you never read in[-1].' },
    },
    {
      id: 'c7', type: 'coding', bugfix: true, title: 'Bug Fix: Range Check Always True', from: 'manager', day: 12, due: { day: 13, minute: 180 }, effort: 60, kind: 'individual', category: 'Coding', fnName: 'inRange',
      brief: '<p>🐞 <b>FIG-233:</b> The safety monitor says every temperature is "in range." Fix <code>bool inRange(int value, int lo, int hi)</code>: true when <code>lo &lt;= value &lt;= hi</code> (inclusive).</p>',
      starter: 'bool inRange(int value, int lo, int hi) {\n    return value >= lo || value < hi;\n}\n',
      tests: [{ call: 'inRange(50, 0, 100)', bool: true, expected: 'true' }, { call: 'inRange(150, 0, 100)', bool: true, expected: 'false' }],
      hidden: [{ call: 'inRange(100, 0, 100)', bool: true, expected: 'true' }, { call: 'inRange(-1, 0, 100)', bool: true, expected: 'false' }, { call: 'inRange(0, 0, 100)', bool: true, expected: 'true' }],
      hints: ['Both conditions must hold: use && not ||.', 'Inclusive upper bound: value <= hi.'],
      wiki: '<b>Logic:</b> "between" is always an AND of two comparisons.',
      peer: { who: 'mei', text: 'That OR means "every number ever." Classic.' },
    },
    IS.T.email({ day: 13, due: { day: 13, minute: 180 }, to: 'Theo Park', toTitle: 'Show Producer', subject: 'Figure Test Delay',
      brief: 'The range-check bug means the new figure\'s safety sign-off moves by one day.',
      terms: ['figure', 'safety', 'fix', 'test', 'date', 'sorry|apolog'] }),
    {
      id: 'r2', type: 'review', title: 'Code Review: Servo Driver', from: 'intern3', day: 13, due: { day: 14, minute: 180 }, effort: 45, kind: 'individual', category: 'Code Review',
      brief: '<p>Mei asks for a safety-focused review of the servo driver.</p>',
      code: `void setServo(int channel, int angle) {
    int pwm = 1000 + angle * 1000 / 180;
    delay(500);
    writePwm(channel, pwm);
}`,
      issues: [
        { text: 'Angle isn\'t clamped, so a bad command can drive the servo past its mechanical stops.', real: true },
        { text: 'delay(500) blocks the control loop, freezing sensors, other servos and the watchdog for half a second.', real: true },
        { text: 'Magic numbers (1000, 180) are undocumented. Name them (MIN_PULSE_US, MAX_ANGLE).', real: true },
        { text: 'channel isn\'t validated, so an out-of-range channel writes to the wrong device.', real: true },
        { text: 'Integer division is not allowed in C++.', real: false },
        { text: 'delay() was removed in C++20.', real: false },
        { text: 'channel should be a float.', real: false },
      ],
      keywords: ['clamp', 'limit', 'block', 'delay', 'magic', 'constant', 'channel', 'valid'],
      hints: ['What does the figure do during that delay?', 'What if angle is 400?'],
      wiki: '<b>Control loops:</b> never block. Use non-blocking timers and state machines.',
      peer: { who: 'arjun', text: 'That delay is why the eyes freeze when the arm moves. Mystery solved.' },
    },
    {
      id: 'c8', type: 'coding', optional: true, title: '⭐ Stretch: Binary Search Pose Table', from: 'mentor', day: 14, due: { day: 17, minute: 180 }, effort: 75, kind: 'individual', category: 'Coding', fnName: 'binarySearch',
      brief: '<p><b>Optional stretch.</b> Pose timestamps are stored sorted. Write <code>int binarySearch(const int a[], int n, int target)</code> returning the index of <code>target</code> or −1, in O(log n).</p>',
      starter: 'int binarySearch(const int a[], int n, int target) {\n    return -1;\n}\n',
      tests: [{ setup: 'int a[5] = {10, 20, 30, 40, 50};', call: 'binarySearch(a, 5, 40)', expected: '3' }, { setup: 'int a[3] = {10, 20, 30};', call: 'binarySearch(a, 3, 25)', expected: '-1' }],
      hidden: [{ setup: 'int a[1]; a[0] = 5;', call: 'binarySearch(a, 1, 5)', expected: '0' }, { setup: 'int a[6] = {1, 3, 5, 7, 9, 11};', call: 'binarySearch(a, 6, 1)', expected: '0' }, { setup: 'int a[6] = {1, 3, 5, 7, 9, 11};', call: 'binarySearch(a, 6, 11)', expected: '5' }, { setup: 'int a[1]; a[0] = 1;', call: 'binarySearch(a, 0, 1)', expected: '-1' }],
      hints: ['lo = 0, hi = n − 1; while (lo <= hi) { mid = lo + (hi − lo) / 2; … }'],
      wiki: '<b>Algorithms:</b> <code>lo + (hi − lo) / 2</code> avoids integer overflow compared to <code>(lo + hi) / 2</code>.',
      peer: { who: 'dante', text: 'I love binary search. It loves off-by-one errors.' },
    },
    IS.T.designDoc({ id: 'w-design2', title: 'Animatronic Safety Monitor', day: 16, due: { day: 17, minute: 180 }, group: 'g2', minWords: 180, metrics: true,
      brief: 'The capstone: firmware that watches motor enables, temperature, current and heartbeats, and puts the figure in a safe state on any fault.',
      terms: ['fault', 'watchdog', 'e-stop', 'temperature', 'current', 'safe state', 'simulator', 'bit'], termsNeeded: 5 }),
    {
      id: 'c9', type: 'coding', title: 'Capstone: Count Enabled Motors', from: 'manager', day: 16, due: { day: 19, minute: 180 }, effort: 105, kind: 'group', group: 'g2', category: 'Coding', fnName: 'bitsSet',
      brief: '<p>Motor enables are stored as bits in one integer (bit 0 = motor 0). Write <code>int bitsSet(int mask)</code> returning how many motors are enabled (set bits) for a non-negative <code>mask</code>.</p>',
      starter: 'int bitsSet(int mask) {\n    return 0;\n}\n',
      tests: [{ call: 'bitsSet(11)', expected: '3' }, { call: 'bitsSet(0)', expected: '0' }],
      hidden: [{ call: 'bitsSet(255)', expected: '8' }, { call: 'bitsSet(1024)', expected: '1' }, { call: 'bitsSet(1023)', expected: '10' }],
      hints: ['while (mask) { count += mask & 1; mask >>= 1; }'],
      wiki: '<b>Bit masks:</b> one integer can hold 32 on/off flags, which is how most hardware registers work.',
      peer: { who: 'mei', text: '11 is 1011 in binary. Three bits. Draw it out!' },
    },
    {
      id: 'c10', type: 'coding', title: 'Capstone: Fault Priority', from: 'intern3', day: 18, due: { day: 20, minute: 180 }, effort: 75, kind: 'group', group: 'g2', category: 'Coding', fnName: 'faultCode',
      brief: '<p>Write <code>int faultCode(int tempC, int currentA, bool estop)</code> returning the highest-priority fault: E-stop pressed → 1; temperature over 80 → 2; current over 15 → 3; otherwise 0 (OK).</p>',
      starter: 'int faultCode(int tempC, int currentA, bool estop) {\n    return 0;\n}\n',
      tests: [{ call: 'faultCode(70, 10, false)', expected: '0' }, { call: 'faultCode(90, 20, true)', expected: '1' }],
      hidden: [{ call: 'faultCode(81, 20, false)', expected: '2' }, { call: 'faultCode(80, 16, false)', expected: '3' }, { call: 'faultCode(80, 15, false)', expected: '0' }],
      hints: ['Check in priority order and return early.', '"Over 80" means > 80, not >= 80.'],
      wiki: '<b>Safety design:</b> the most severe fault wins, and the E-stop always wins.',
      peer: { who: 'arjun', text: 'Boundary values: 80 °C is fine, 81 °C is a fault.' },
    },
    {
      id: 'c11', type: 'coding', urgent: true, bugfix: true, title: '🚨 URGENT: Twitching Arm Mid-Show', from: 'manager', day: 22, due: { day: 22, minute: 120 }, effort: 40, kind: 'individual', category: 'Coding', fnName: 'filterReading',
      brief: '<p>🚨 <b>SEV-2:</b> A figure\'s arm twitches during shows because sensor glitches pass straight through. Fix <code>int filterReading(int prev, int raw, int maxJump)</code> <b>by 11:00 AM</b>: if <code>raw</code> differs from <code>prev</code> by more than <code>maxJump</code>, return <code>prev</code>; otherwise return <code>raw</code>.</p>',
      starter: 'int filterReading(int prev, int raw, int maxJump) {\n    return raw;\n}\n',
      tests: [{ call: 'filterReading(100, 102, 5)', expected: '102' }, { call: 'filterReading(100, 180, 5)', expected: '100' }],
      hidden: [{ call: 'filterReading(100, 95, 5)', expected: '95' }, { call: 'filterReading(100, 94, 5)', expected: '100' }, { call: 'filterReading(-10, -40, 20)', expected: '-10' }],
      hints: ['Use abs(raw - prev) > maxJump from <cstdlib>.', 'A difference exactly equal to maxJump is allowed.'],
      wiki: '<b>Incident runbook:</b> the smallest safe fix first; the show resumes after a reduced-speed test.',
      peer: { who: 'lily', text: 'The next show is at noon. Guests are waiting.' },
    },
    IS.T.postmortem({ title: 'Arm Twitch During Show', day: 22, due: { day: 23, minute: 180 }, terms: ['sensor', 'glitch|spike', 'filter', 'figure', 'test', 'monitor|alert', 'maxjump|threshold'] }),
    {
      id: 'c12', type: 'coding', title: 'Capstone: Watchdog Missed Beats', from: 'intern4', day: 21, due: { day: 23, minute: 180 }, effort: 90, kind: 'group', group: 'g2', category: 'Coding', fnName: 'missedBeats',
      brief: '<p>Each controller sends a heartbeat. Given increasing timestamps (ms), write <code>int missedBeats(const int t[], int n, int timeout)</code> returning how many gaps between consecutive heartbeats are <b>greater than</b> <code>timeout</code>.</p>',
      starter: 'int missedBeats(const int t[], int n, int timeout) {\n    return 0;\n}\n',
      tests: [{ setup: 'int t[4] = {0, 100, 250, 300};', call: 'missedBeats(t, 4, 120)', expected: '1' }, { setup: 'int t[1]; t[0] = 0;', call: 'missedBeats(t, 1, 50)', expected: '0' }],
      hidden: [{ setup: 'int t[3] = {0, 50, 100};', call: 'missedBeats(t, 3, 50)', expected: '0' }, { setup: 'int t[4] = {0, 500, 1000, 1001};', call: 'missedBeats(t, 4, 100)', expected: '2' }],
      hints: ['Compare t[i] - t[i-1] for i from 1 to n − 1.'],
      wiki: '<b>Watchdogs:</b> if the heartbeat stops, the figure must go to a safe pose automatically.',
      peer: { who: 'arjun', text: 'The simulator sends beats every 100 ms. Real boards are messier.' },
    },
    {
      id: 'c13', type: 'coding', title: 'Capstone: Is Motor Enabled?', from: 'mentor', day: 24, due: { day: 26, minute: 180 }, effort: 60, kind: 'group', group: 'g2', category: 'Coding', fnName: 'motorEnabled',
      brief: '<p>Write <code>bool motorEnabled(int mask, int motor)</code>: true if bit <code>motor</code> of <code>mask</code> is set. Motors are numbered 0–15; anything outside that range → false.</p>',
      starter: 'bool motorEnabled(int mask, int motor) {\n    return false;\n}\n',
      tests: [{ call: 'motorEnabled(5, 2)', bool: true, expected: 'true' }, { call: 'motorEnabled(5, 1)', bool: true, expected: 'false' }],
      hidden: [{ call: 'motorEnabled(1, 0)', bool: true, expected: 'true' }, { call: 'motorEnabled(65535, 16)', bool: true, expected: 'false' }, { call: 'motorEnabled(8, -1)', bool: true, expected: 'false' }],
      hints: ['Validate motor first.', 'return (mask & (1 << motor)) != 0;'],
      wiki: '<b>Bits:</b> <code>1 &lt;&lt; k</code> builds a mask with only bit k set.',
      peer: { who: 'dante', text: 'Shifting by a negative number is undefined. Check the range first!' },
    },
    IS.T.status({ day: 25, terms: ['capstone', 'watchdog', 'fault', 'postmortem', 'simulator', 'figure'] }),
    IS.T.readme({ day: 26, due: { day: 28, minute: 180 }, project: 'Animatronic Safety Monitor', terms: ['faultcode', 'watchdog', 'bitsset|mask', 'simulator', 'c++', 'safe'] }),
    IS.T.finalPres({ project: 'Animatronic Safety Monitor', terms: ['fault', 'watchdog', 'safe', 'motor', 'temperature', 'guest', 'simulator'], questions: [
      { who: 'harriet', q: 'What does a guest experience if a fault happens mid-show?', options: [
        { text: 'The figure smoothly returns to a neutral pose and the show continues with lighting and audio, so most guests never notice anything went wrong.', pts: 10 },
        { text: 'The figure stops wherever it is.', pts: 3 },
        { text: 'Nothing, faults are rare.', pts: 1 },
        { text: 'The show is canceled.', pts: 4 }] },
      { who: 'harriet', q: 'How do you know it\'s safe enough?', options: [
        { text: 'Unit tests for every fault path, 10,000 randomized simulator runs overnight, and a reduced-speed test on the real figure with the E-stop in reach.', pts: 10 },
        { text: 'We tested it a few times.', pts: 3 },
        { text: 'The code looks correct.', pts: 1 },
        { text: 'Safety is the hardware team\'s job.', pts: 0 }] },
      { who: 'manager', q: 'What\'s the biggest risk?', options: [
        { text: 'Sensor noise causing false faults and needless show stops. We added filtering and require a fault to persist for several ticks.', pts: 10 },
        { text: 'None.', pts: 0 },
        { text: 'The boards might arrive late.', pts: 4 },
        { text: 'Dante.', pts: 0 }] },
      { who: 'mentor', q: 'Why no dynamic memory allocation?', options: [
        { text: 'Heap fragmentation and allocation failures are unpredictable. Fixed arrays keep timing and memory deterministic.', pts: 10 },
        { text: 'We forgot to add it.', pts: 1 },
        { text: 'Heap is slower in C++.', pts: 4 },
        { text: 'Vectors are illegal.', pts: 0 }] },
    ] }),
    IS.T.selfEval({ day: 30, due: { day: 31, minute: 180 }, terms: ['firmware', 'capstone', 'safety', 'team', 'test', 'deadline'] }),
  ],
  interview: {
    coding: [
      { id: 'i-cpp1', title: 'Factorial', fnName: 'factorial',
        brief: '<p>Write <code>int factorial(int n)</code> for 0 ≤ n ≤ 12. <code>factorial(0)</code> is 1.</p>',
        starter: 'int factorial(int n) {\n    return 0;\n}\n',
        tests: [{ call: 'factorial(5)', expected: '120' }, { call: 'factorial(0)', expected: '1' }],
        hidden: [{ call: 'factorial(1)', expected: '1' }, { call: 'factorial(10)', expected: '3628800' }] },
      { id: 'i-cpp2', title: 'Is Prime?', fnName: 'isPrime',
        brief: '<p>Write <code>bool isPrime(int n)</code>. Numbers below 2 are not prime.</p>',
        starter: 'bool isPrime(int n) {\n    return false;\n}\n',
        tests: [{ call: 'isPrime(7)', bool: true, expected: 'true' }, { call: 'isPrime(8)', bool: true, expected: 'false' }],
        hidden: [{ call: 'isPrime(1)', bool: true, expected: 'false' }, { call: 'isPrime(2)', bool: true, expected: 'true' }, { call: 'isPrime(97)', bool: true, expected: 'true' }, { call: 'isPrime(91)', bool: true, expected: 'false' }] },
      { id: 'i-cpp3', title: 'Reverse Digits', fnName: 'reverseDigits',
        brief: '<p>Write <code>int reverseDigits(int n)</code> for n ≥ 0. <code>1230</code> → <code>321</code>.</p>',
        starter: 'int reverseDigits(int n) {\n    return n;\n}\n',
        tests: [{ call: 'reverseDigits(123)', expected: '321' }, { call: 'reverseDigits(1230)', expected: '321' }],
        hidden: [{ call: 'reverseDigits(0)', expected: '0' }, { call: 'reverseDigits(7)', expected: '7' }] },
    ],
    concepts: [
      { q: 'What is 9 / 4 when both are int?', options: ['2.25', '2', '3', '2.0'], answer: 1 },
      { q: 'Accessing a[10] on int a[10] is:', options: ['The last element', 'Undefined behavior', 'Always zero', 'A compiler warning only'], answer: 1 },
      { q: 'Passing `int& x` to a function means:', options: ['A copy is made', 'The function can modify the caller\'s variable', 'x is constant', 'x is an array'], answer: 1 },
      { q: 'Why avoid `new`/`malloc` in real-time firmware?', options: ['It is illegal', 'Unpredictable timing and fragmentation', 'It uses too little memory', 'No reason'], answer: 1 },
      { q: 'What does `x >> 1` do for non-negative x?', options: ['Doubles it', 'Halves it (integer)', 'Negates it', 'Nothing'], answer: 1 },
      { q: 'Which loop reads every element of int a[n]?', options: ['for (i = 1; i <= n; i++)', 'for (i = 0; i < n; i++)', 'for (i = 0; i <= n; i++)', 'for (i = n; i > 0; i--)'], answer: 1 },
      { q: 'A `const` parameter:', options: ['Can\'t be modified inside the function', 'Is faster', 'Must be global', 'Is always a pointer'], answer: 0 },
      { q: 'What does a watchdog timer do?', options: ['Counts pets', 'Resets or safes the system if the software stops responding', 'Speeds up loops', 'Logs errors'], answer: 1 },
    ],
  },
  training: {
    lessons: [
      { title: 'C-style C++ essentials', html: '<pre>int sum(const int a[], int n) {\n    int total = 0;\n    for (int i = 0; i &lt; n; i++) total += a[i];\n    return total;\n}</pre><p>Arrays don\'t know their size. Always pass <code>n</code>, and valid indexes are 0 … n−1.</p>' },
      { title: 'Integer math & bits', html: '<ul><li><code>7 / 2 == 3</code> and <code>7 % 2 == 1</code></li><li><code>x % 10</code> is the last digit; <code>x / 10</code> drops it</li><li><code>mask &amp; (1 &lt;&lt; k)</code> tests bit k; <code>mask &gt;&gt;= 1</code> shifts right</li></ul>' },
      { title: 'Firmware habits', html: '<p>Clamp every input, never block the loop, no heap allocation, and treat every sensor value as possibly wrong. Interviewers love hearing you think about failure modes.</p>' },
    ],
  },
});
